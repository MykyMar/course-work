<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Cross Browser Suite">
    <test name="Run Cucumber Tests">
        <parameter name="browser" value="${browser}" /> <!-- Taking a parameter from the command line -->
        <classes>
            <class name="cucumberExamples.RunCucumberTest" />
        </classes>
    </test>
</suite>